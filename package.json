{
  "name": "test-driven-login",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "cypress": "cypress open",
    "test:functional": "npm-run-all --parallel start cypress"
  },
  "devDependencies": {
    "cypress": "^3.0.1",
    "npm-run-all": "^4.1.3",
    "react-testing-library": "^3.1.3"
  }
}
